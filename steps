steps:

1. download postgres & postgresql 9.5
2. edit sysctl.conf as indicated in postgreseql readme
3. in pgAdmin, create new database (e.g. makesense, owner=postgres)
4. edit path (will need to add something like export PATH=/Applications/Postgres.app/Contents/Versions/9.5/bin:$PATH)
5. on command line, do `pg_restore --verbose --clean --no-acl --no-owner -h localhost -U postgres -C Documents/Gits/Makesense/data/latest.dump`
6. on command line, do `psql -d makesense -U postgres -f Documents/Gits/Makesense/data/db.sql`


to login to postgres, do `sudo -u postgres psql postgres`

-d makesense -U postgres - f path/to/file
